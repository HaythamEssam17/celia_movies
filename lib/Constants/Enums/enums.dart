enum DeviceType { mobile, tablet, desktop }

enum CustomStatusCodeErrorType {
  init,
  server,
  internet,
  gatWay,
  redirection,
  connectTimeout,
  receiveTimeout,
  sendTimeout,
  badRequest,
  unExcepted,
  unVerified,
  notFound,
}
